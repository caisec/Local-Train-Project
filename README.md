# Local-Train-Project
Local Train Booking System is a university web project built with PHP (MVC architecture) that simulates a local train ticket booking system. It allows users to register, log in, view train schedules, and book tickets,admins can manage users,recharge balances,add schedules,control bookings.The system uses HTML,CSS,Bootstrap,JavaScript,SQL Server.
